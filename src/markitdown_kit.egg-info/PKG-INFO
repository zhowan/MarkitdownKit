Metadata-Version: 2.4
Name: markitdown-kit
Version: 0.1.0
Summary: A simple GUI app for converting files to Markdown with MarkItDown.
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: markitdown[docx,pdf,pptx,xls,xlsx]<0.2,>=0.1.5

# MarkItDown Kit

一个尽量傻瓜式的本地小工具，基于微软开源项目 [MarkItDown](https://github.com/microsoft/markitdown)，把常见文件一键转换成 Markdown。

## 现在已经有的功能

- 图形界面，不需要手敲命令
- 支持一次添加多个文件批量转换
- 可选统一输出目录
- 也可以直接输出到原文件所在目录
- 自动保留原文件名并生成 `.md`
- 内置运行日志，失败时方便定位原因

## 支持的文件类型

底层能力来自 `markitdown`。根据官方 README 和 PyPI 当前版本 `0.1.5`（发布日期：2026-02-20），项目本身支持很多格式。

这个 GUI 当前默认安装的是一组更稳妥、对日常办公最有用的格式支持：

- PDF
- Word `.docx`
- PowerPoint `.pptx`
- Excel `.xlsx` / `.xls`
- HTML
- CSV / JSON / XML 等文本格式

之所以没有直接启用 `markitdown[all]`，是因为当前机器上的 `Python 3.14.3` 与其中个别附加依赖存在兼容性问题，会影响首次安装成功率。后续如果你需要，我也可以再帮你单独补上图片 OCR、音频等扩展能力。

## 本地运行

### 方法 1：双击启动

直接双击项目根目录下的 [start.command](/Users/wong/CodeHub/WongsCodesx/MarkitdownKit/start.command)。

它会自动做这些事：

1. 创建 `.venv`
2. 安装依赖
3. 启动图形界面

### 方法 2：终端启动

```bash
cd /Users/wong/CodeHub/WongsCodesx/MarkitdownKit
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -e .
python -m markitdown_kit
```

## 项目结构

- [pyproject.toml](/Users/wong/CodeHub/WongsCodesx/MarkitdownKit/pyproject.toml)：项目依赖和入口
- [src/markitdown_kit/app.py](/Users/wong/CodeHub/WongsCodesx/MarkitdownKit/src/markitdown_kit/app.py)：GUI
- [src/markitdown_kit/converter.py](/Users/wong/CodeHub/WongsCodesx/MarkitdownKit/src/markitdown_kit/converter.py)：对 `MarkItDown` 的转换封装
- [start.command](/Users/wong/CodeHub/WongsCodesx/MarkitdownKit/start.command)：macOS 双击启动脚本

## 下一步可以继续做

如果你愿意，我下一轮可以继续帮你做下面任意一个版本升级：

1. 打包成真正的 macOS `.app`
2. 加拖拽上传
3. 加“转换整个文件夹”
4. 加文件预览和导出成功提示优化
